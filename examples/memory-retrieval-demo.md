# Memory retrieval demo

1. The session completes a debugging task.
2. Consolidation extracts an episodic memory:
   - task: fixed import path mismatch
   - project: demo-app
   - successful action: update package layout
3. The next related session retrieves this memory through lexical + semantic fusion.
4. The memory packager injects only the top snippets under token budget.
