"""Initial schema reference for sentinel-agent.

This file mirrors the schema created by `DatabaseManager.initialize()` and documents the
first durable storage version for sessions, messages, memories, traces, approvals, tools,
and embedding cache metadata.
"""

REVISION = "0001_initial_schema"
