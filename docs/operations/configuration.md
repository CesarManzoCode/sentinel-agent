# Configuration

Configuration precedence:

1. built-in defaults
2. profile defaults
3. config file
4. environment variables
5. explicit CLI flags

The settings model resolves XDG-compatible paths, workspace roots, model settings, telemetry posture, and safety options.
